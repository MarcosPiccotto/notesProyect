const express = require("express");
const router = express.Router();

const noteController = require("../controllers/noteController");

// Cambiar los get estos por los que estan en el controlador
router.route("/").get(noteController.getNotes);

//Crear una nota
router.route("/createNote").post(noteController.createNote);

// obtener una nota directa, aunque no se si directamente ver
//Obtener todas las notas no se hacer esto o que sea el main
//Eliminar una nota
//Modificar una nota


module.exports = router;